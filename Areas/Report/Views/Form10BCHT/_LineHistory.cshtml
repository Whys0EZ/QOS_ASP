@model IEnumerable<QOS.Areas.Report.Models.LineHistoryDataForm6>

<div class="alert">
    <table class="table  table-sm table-bordered mt-2">
        <thead class="table-dark">
            <tr>
                <th >STT</th>
                <th >ID Báo Cáo</th>
                <th >MO</th>
                <th >Màu</th>
                <th>AQL</th>
                <th>Tổng SL</th>
                <th >SL Lỗi/SL Kiểm</th>
                <th >Lần kiểm</th>
                <th >OQL</th>
                <th >Người tạo</th>
                <th>Thời gian kiểm</th>
            </tr>
        </thead>
        <tbody>
        @if (Model != null && Model.Any())
        {

            var idx = 1;
            foreach (var item in Model)
            {
                
                <tr class="@(item.Status == "green" ? "table-success" : item.Status == "red" ? "table-danger" : item.Status == "yellow" ? "table-warning" : "")">
                    <td>@idx</td>
                    <td> <span style="text-decoration: underline; cursor: pointer; color:blue;"
                                onclick="showDetails('@item.ID')">@item.ID</span></td>
                    <td>@item.MO</td>
                    <td>@item.Color</td>
                    <td>@item.AQL</td>
                    <td>@item.Check_QTY</td>
                    <td>@item.Total_Fault_QTY/ @item.QTY</td>
                    <td>@item.Audit_Time</td>
                    <td>@item.OQL %</td>
                    <td>@item.UserUpdate - @item.FullName</td>
                    <td>@item.LastUpdate</td>
                </tr>
                idx++;
            }
        }
        else
        {
            <tr>
                <td colspan="9" class="text-center text-warning">
                    Không có dữ liệu cho line này.
                </td>
            </tr>
        }
        </tbody>
    </table>
</div>
 @* modal-fullscreen *@
<div class="modal fade" id="Form10Modal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-xl">
        <div class="modal-content">
            <div class="modal-header bg-info text-white text-center text-uppercase">
                <h5 class="modal-title">Chi tiết báo cáo</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body" id="ModalRP_Form10">
                <!-- Nội dung chi tiết sẽ load vào đây -->
            </div>
        </div>
    </div>
</div>
<script src="~/js/jquery-3.6.0.min.js"></script>
<script>
function showDetails(id) {
    $.get('@Url.Action("DetailForm10", "Form10BCHT", new { area = "Report" })',
        { id: id },
        function (data) {
            if (data) {
                // inset info
                $("#ModalRP_Form10").html(data);
                var modal = new bootstrap.Modal(document.getElementById('Form10Modal'));
                var unit = $("#ModalRP_Form10").find("#UnitField").val();
                $(".modal-title").text("Chi tiết báo cáo - Chuyền : " + unit);

                modal.show();
            }
        });
}

</script>